A *palindrome* is a string that reads the same from left to right as it does from right to left.
Given a string, svg, of svg lowercase English letters, we define a *svg-length rotation* as cutting the first svg characters from the beginning of svg and appending them to the end of svg. For each svg, there are svg possible svg-length rotations (where svg). See the *Explanation* section for examples.
Given svg and svg, find all svg svg-length rotations of svg; for each rotated string, svg, print the maximum possible length of any palindromic substring of svg on a new line.
**Input Format**
The first line contains an integer, svg (the length of svg).
The second line contains a single string, svg.
**Constraints**
- svg
- svg
- svg
**Output Format**
There should be svg lines of output, where each line svg contains an integer denoting the maximum length of any palindromic substring of rotation svg.
**Sample Input 0**
```
13
aaaaabbbbaaaa
```
**Sample Output 0**
```
12
12
10
8
8
9
11
13
11
9
8
8
10
```
**Sample Input 1**
```
7
cacbbba
```
**Sample Output 1**
```
3
3
3
3
3
3
3
```
**Sample Input 2**
```
12
eededdeedede
```
**Sample Output 2**
```
5
7
7
7
7
9
9
9
9
7
5
4
```
**Explanation**
Consider *Sample Case 1*, where svg.
The possible rotations, svg, for string svg are:
svg.
svg
svg
svg
svg
svg
svg
The longest palindromic substrings for each svg are:
svg and svg, so we print their length (svg) on a new line.
svg, so we print its length (svg) on a new line.
svg and svg, so we print their length (svg) on a new line.
svg and svg, so we print their length (svg) on a new line.
svg and svg, so we print their length (svg) on a new line.
svg and svg, so we print their length (svg) on a new line.
svg and svg, so we print their length (svg) on a new line.

import java.util.*;
public class Solution {
    static int longestPalindrome(String s) {
        int max = 1;
        for (int i = 0; i < s.length(); i++) {
            max = Math.max(max, expand(s, i, i));
            max = Math.max(max, expand(s, i, i + 1));
        }
        return max;
    }
    static int expand(String s, int left, int right) {
        while (left >= 0 && right < s.length()
                && s.charAt(left) == s.charAt(right)) {
            left--;
            right++;
        }
        return right - left - 1;
    }
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int n = sc.nextInt();
        String s = sc.next();
        for (int i = 0; i < n; i++) {
            String rotation = s.substring(i) + s.substring(0, i);
            System.out.println(longestPalindrome(rotation));
        }
    }
}
